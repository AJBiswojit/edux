# Frontend talks to the FastAPI app over HTTP only.
# Include the /v1 prefix. Do not put DATABASE_URL, SECRET_KEY, or any
# backend secret in the Vite environment.
VITE_API_BASE_URL=http://localhost:8000/v1
