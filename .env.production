# Production build config (baked in at `npm run build`).
# API is same-origin: nginx proxies /v1 -> backend, so no host/IP is hardcoded.
VITE_USE_MOCK=false
VITE_API_BASE_URL=/v1
VITE_MOCK_FALLBACK=false
